# Environment variables for the application
NODE_ENV=development
PORT=3000
API_PREFIX=/api/v1

# Database Configuration
# For development (SQLite) - uncomment the line below
# DATABASE_URL="file:./dev.db"

# For production (PostgreSQL) - use the format below
DATABASE_URL="postgresql://username:password@localhost:5432/express_learning_db?schema=public"

# JWT Configuration (for when authentication is implemented)
JWT_SECRET=your-super-secret-jwt-key-here
JWT_EXPIRES_IN=7d

# Redis Configuration (for caching when implemented)
REDIS_URL="redis://localhost:6379"

# External API keys and other secrets would go here
